{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "backend",
    "frontend",
    "lib/*"
  ],
  "scripts": {
    "dev": "npx concurrently -n api,web -c blue,green \"npm run dev -w @workspace/backend\" \"npm run dev -w @workspace/hr-platform\"",
    "start": "npm run dev",
    "build": "npm run typecheck && npm run build --workspaces --if-present",
    "typecheck:libs": "tsc --build",
    "typecheck": "npm run typecheck:libs && npm run typecheck --workspaces --if-present"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^10.0.3",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
